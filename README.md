# retriever-chat
In order to run the file you need to run server.py on one computer and have users connect via running client_gui.py or client.py on another computer. The one person who runs server.py needs to give the IP address to the other users. You can all connect and broadcast chats together and receive real time messages. 
